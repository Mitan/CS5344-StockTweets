requirement - pandas library
format for input 
python DataCollector.py start_date end_date symbol mode output_file_path

date is in format YYYY-MM-DD
mode is either 'V' (volume) or 'C' (close date)


format for input 
python Normalizer.py input_file output_file mode

mode is either 'V' (volume) or 'C' (close date)